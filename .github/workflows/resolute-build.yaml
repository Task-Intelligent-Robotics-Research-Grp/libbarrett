name: resolute-build
on:
  push
  workflow_dispatch:
  
jobs:
  resulute-build:
    uses: ./.github/workflows/build.yaml
    with:
      os: ubuntu-26.04

permissions:
  contents: write
  pages: write
  id-token: write
